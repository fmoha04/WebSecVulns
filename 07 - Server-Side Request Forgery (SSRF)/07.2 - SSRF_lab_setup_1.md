> LAB-1 Setup

*Visual Image*

<img width="732" height="299" alt="image" src="https://github.com/user-attachments/assets/eb365152-bbf2-4372-959a-2b025812260a" /><br>

-parrot-vm-
```
docker run -dit -p 80:80 --cap-add=NET_ADMIN --cap-add=NET_ADMIN --name ssrf_first_lab ubuntu
```
```
docker exec -it $(docker ps -aq | tail -n 2 | head -n 1) bash
```

-ubuntu-container-
```
apt update && apt install -y nano apache2 php python3 lsof net-tools curl git
```
```
service apache2 start
```
```
sed -i 's/allow_url_include = Off/allow_url_include = On/g' /etc/php/$(ls /etc/php/)/apache2/php.ini
```
```
service apache2 restart
```
```
agregamos los archivos login1.html & login2.html & utility.php a la ruta /var/www/html
```

-parrot-vm-
```
firefox "http://localhost:80"
```



