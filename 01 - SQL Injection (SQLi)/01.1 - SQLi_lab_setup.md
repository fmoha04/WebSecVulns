> Required Packages
```bash
sudo apt update && sudo apt install -y mariadb-server apache2 php-mysql
sudo service mysql start && sudo service apache2 start
```
> MySQL Access
```bash
mysql -u root -p
```
> Database Config
```sql

create database db;
use db;

create table users(
	id int(32),
	username varchar(32),
	password varchar(32)
);

insert into users(id, username, password)
values
(1,'admin', 'admin123$!p@$$'),
(2,'fmoha', 'fmoha123'),
(3,'omar', 'omarelhacker1313');

create user 'fmoha'@'localhost' identified by 'fmoha123';
grant all privileges on db.* to 'fmoha'@'localhost';

```

